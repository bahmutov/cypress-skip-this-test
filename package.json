{
  "name": "cypress-skip-this-test",
  "version": "1.0.0",
  "description": "Skips the current test if it depends on the previous failed test",
  "main": "src/index.js",
  "files": [
    "src"
  ],
  "scripts": {
    "test": "cypress-expect run --expect-exactly results.json"
  },
  "keywords": [
    "cypress",
    "cypress-plugin"
  ],
  "author": "Gleb Bahmutov",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^13.14.1",
    "cypress-expect": "^3.1.2",
    "prettier": "^3.3.3"
  }
}
